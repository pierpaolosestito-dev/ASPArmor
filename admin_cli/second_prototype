#@ superapparmor,1

abi <abi/3.0>,

include <tunables/global>

profile /usr/bin/forse.sh flags=(complain) {
include <a/mappings> #@+ignore
    
    include <.usr.bin.forse.sh/mappings> #@+ignore
    
    include <abstractions/base>
    ###include <abstractions/base>
    include <abstractions/bash>
    ##include <abstractions/base>
    #@+selectable{alias="a",groups=["sudo","admin"]} /usr/bin/bash ix,
    #@+selectable{alias='b',users=["pierpaolo-sestito","pierpaolodue","pierpaolotre"]} /usr/bin/forse.sh r,
    #@selectable{alias='x',groups=['sudo','admin']}
    #/usr/bin/blabla ix,
    #/usr/bin/ci ix,
    #/usr/bin/ci ix,
    #@end
    #@+selectable{alias='c'} /usr/bin/date ix, /usr/bin/sleep ix,
    /usr/bin/sleep ix,
    #/usr/bin/sleep ix,
    /usr/bin/ci ix,
    
    profile pieruzzo {
        /usr/bin/sleep ix,
        
        
        
    }
    
    profile pieruzzo2 {
        /usr/bin/sleep2 ix,
        
    }
}