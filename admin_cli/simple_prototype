#@ superapparmor,1

abi <abi/3.0>,
#abc, abi <abi/3.0>   ,
include <tunables/global>

profile /usr/bin/forse.sh flags=(complain) {
    
    include <.usr.bin.forse.sh/mappings> #@+ignore
    
    include <abstractions/base>
    include <abstractions/bash> #include <abstractions/bash>
    #@+selectable{alias="a",groups=["sudo","admin"]} /usr/bin/bash ix,
    #@+selectable{alias='b',groups=["pierpaolodue","pierpaolotre"]} /usr/bin/forse.sh r,
    #@selectable{alias='x',groups=['sudo','admin']}
    #/usr/bin/blabla ix,
    #/usr/bin/ci ix,
    #/usr/bin/ahi ix,/usr/bin/bi ix,
    #@end
    #@selectable{alias='d',groups=['sudo','admin']}
    #/usr/bin/blabla ix,
    #/usr/bin/ci ix,
    #/usr/bin/ahi ix,/usr/bin/bi ix,
    #@end
    #@+selectable{alias='c'} /usr/bin/date ix, /usr/bin/sleep ix,
    /usr/bin/sleep ix,
    #/usr/bin/sleep ix,
    /usr/bin/forse.sh r,
    /usr/bin/ci ix,
    /usr/bin/sleep ix,/usr/bin/ci ix,

    profile pieruzzo {
        #include <abs/abs>
        /usr/bin/sleep ix,
        /usr/bin/sleep ix,
        /usr/bin/ahi ix, /usr/bin/bhi ax,
    }


}